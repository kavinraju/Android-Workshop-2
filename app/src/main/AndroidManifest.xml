<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="skr.app.dev.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- For Network State Access -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".NewsDetailsActivity"
            android:label="@string/title_activity_news_details"
            android:theme="@style/AppTheme.NoActionBar"/>

        <!-- You will change the  android:name tag in activity tag below while working - just for making the thing easier -->
        <activity
            android:name=".NewsActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <!-- Place your intent filter here to view the News Activity
            *DO NOT HAVE INTENT FILTER FOR TWO ACTIVITIES* -->
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <!-- Place your intent filter here to view the RecyclerView Example -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>